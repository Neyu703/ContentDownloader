// Jest has no CSS transform — AeroShards.tsx (vendored, coverage-excluded) imports its own .css
// file directly, which only Metro's real web bundler understands. Map it to an empty module here.
module.exports = {};
